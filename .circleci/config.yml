# Huh?!
# Foo
version: 2

jobs:
  test:
    docker:
      - image: busybox
    steps:
      - run: 'true'

  release:
    docker:
      - image: circleci/node:8
    steps:
      - checkout
      - run: yarn install
      - run: yarn run semantic-release

workflows:
  version: 2
  test-and-release:
    jobs:
      - test
      - release:
          requires:
            - test
